{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "cd /c/Users/frede/AppData/Local/Temp/impro-repo && npx tsc --noEmit 2>&1 | head -5"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -q '\\.env$'; then echo 'BLOCKED: .env est protégé' && exit 1; fi"
      }
    ]
  }
}
